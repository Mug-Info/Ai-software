in html file address to the config file :  example (""const config = await loadConfig('first/config.json') || fallbackConfig;"")
place contents in folder with config (content addressing in config)
html file loads contents from config file
